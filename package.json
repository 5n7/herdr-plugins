{
  "name": "herdr-plugins",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build --target=bun plugins/herdr-even-layout/src/main.ts --outfile dist/herdr-even-layout/plugin.js",
    "build:standalone": "bun build --compile --target=bun plugins/herdr-even-layout/src/main.ts --outfile dist/herdr-even-layout/plugin && bash scripts/sign-standalone.sh",
    "check": "bun run format:check && bun run lint && bun run typecheck && bun run test && bun run build && bun run build:standalone && bun run smoke:standalone",
    "format": "oxfmt --write . && mise exec -- shfmt -w plugins/herdr-even-layout/plugin scripts/sign-standalone.sh scripts/smoke-standalone.sh",
    "format:check": "oxfmt --check . && mise exec -- shfmt -d plugins/herdr-even-layout/plugin scripts/sign-standalone.sh scripts/smoke-standalone.sh",
    "lint": "oxlint --deny-warnings . && mise exec -- shellcheck --shell=bash plugins/herdr-even-layout/plugin scripts/sign-standalone.sh scripts/smoke-standalone.sh",
    "smoke:standalone": "./scripts/smoke-standalone.sh",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "oxfmt": "^0.68.0",
    "oxlint": "^1.83.0",
    "typescript": "^7.0.2"
  }
}
